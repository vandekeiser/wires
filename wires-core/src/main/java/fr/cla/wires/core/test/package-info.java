/**
 * Module fr.cla.wires.core exports it test classes too,
 * to allow test-scoped-dependencies and white-box-testing at the same time.
 * It's a "main & test module", don't know yet if that pattern is acceptable.
 */
package fr.cla.wires.core.test;